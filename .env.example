# Copier ce fichier en `.env` puis ajuster si besoin :  cp .env.example .env

# Identifiants PostgreSQL - utilisés par les services `db` et `api` (docker-compose).
POSTGRES_USER=obrail
POSTGRES_PASSWORD=obrail
POSTGRES_DB=obrail
